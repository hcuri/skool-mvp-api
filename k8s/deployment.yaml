apiVersion: apps/v1
kind: Deployment
metadata:
  name: skool-mvp-app
  namespace: apps
  labels:
    app: skool-mvp-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: skool-mvp-app
  template:
    metadata:
      labels:
        app: skool-mvp-app
    spec:
      containers:
        - name: skool-mvp-app
          image: hcuri/skool-mvp-app:dev3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: LOG_LEVEL
              value: "info"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: skool-mvp-db
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
